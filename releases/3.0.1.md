# Ąlpha Şquad UI 3.0.1

- The Add-Ons menu displays the complete **Ąlpha Şquad UI** name, with orange branding and white UI. Compact blue author colors keep **@SeRuM1** readable.
- Expanding the entry shows all seven libraries used by the complete group feature set, including their dependencies.
- Green identifies installed and enabled libraries. Red states explain missing libraries, disabled addons, required updates or unavailable dependencies.
- States follow the native Add-Ons character filter and refresh with the menu. No addon checkbox or sharing preference is changed.
- Local Ultimate tracking remains available without group libraries. The static description lists library names before login or when the suite is not loaded; live status colors require it to be loaded.

Install the updated `AlphaSquadUI` folder and reload the UI. No SavedVariables reset is needed; the Build Share companion is unchanged. Validate the menu's appearance in ESO after installation.
