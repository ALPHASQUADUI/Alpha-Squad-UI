# Project context

Ąlpha Şquad UI 3.3.1 combines group preparation, visual build inspection and personal/group Ultimate tracking with integrated optional Overload behavior. The public maintainer identity is **@SeRuM1**.

The interface favors compact visual lists, native ESO artwork, exact item/morph tooltips and clear evidence states. Dashboard owns module activation and Cross-sync; Libraries owns sharing and dependency guidance; the sidebar MOVE HUD action owns temporary placement. Libraries and the independent Core settings shell remain available when every tracking module is disabled. MOVE HUD also owns corner scaling, edge reshaping, background opacity and panel reset.

Support Coverage focuses on two configurable contexts, Trial and Dungeon, with Buffs, Debuffs, Group Sets and Group Mythics. It evaluates qualifying equipped sources before combat, with duplicate providers and exact source explanations on a single compact page. Build set headlines use effective weapon-bar totals, with excess warnings only from known native bonus thresholds. Builds integrates food and potion readiness; there is no separate Food Check page, combat report or uptime history.

Personal ULT and Overload share one HUD and lifecycle. New installations default to AUTO; existing display modes, including BOTH, are preserved. Overload options live under ULT Tracker. Three saved themes apply across the suite: Ember Classic, Tactical Compact and default Obsidian Studio. About opens the verified direct Discord invitation through ESO confirmation; there is no embedded HTML, widget or fabricated member count.

Existing account/server settings remain the default. Character-specific profiles preserve current placement when Cross-sync is disabled. Supported sharing starts OFF on a fresh installation and requires an explicit choice. Existing saved choices and native OFF states remain respected. Sharing is separate from layout and tracking activation. Native group membership alone cannot reveal complete remote builds.

Refer to the changelog for package changes, architecture for module boundaries and the ESO checklist for acceptance evidence. Maintainer review and actual client checks remain required before publishing a release.
